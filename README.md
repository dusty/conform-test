Playing with conform.

NOTE: Watch the javascript console to see the client side logging, watch the terminal to see the server logging (what is returned in the action)

```
pnpm install
pnpm run dev
```
